export const TENANT_DB_NAME_PREFIX = 'tenant';
export const TENANT_DB_SAFE_NAME_REGEX = /^\w+$/;

/** Bump when tenant DDL changes; used with company.tenant_schema_version. */
export const TENANT_SCHEMA_VERSION = 1;

export const DEFAULT_TENANT_POOL_MAX = 32;
